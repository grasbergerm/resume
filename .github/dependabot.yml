version: 2
updates:
  # Keeps the SHA-pinned GitHub Actions up to date (updates the pin and the # version comment).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
